"""Grounded prompt for explaining an already calculated city scenario."""

import json


# Names from the task specification; calculations continue to use JSON data.
INDICATOR_LABELS = {
    "T1": "разгрузка дорог",
    "T2": "доступность общественного транспорта",
    "E1": "озеленение",
    "E2": "качество воздуха",
    "S1": "школы и детские сады",
    "S2": "поликлиники и первичная помощь",
    "B1": "безопасность улиц",
    "B2": "безопасность дорожного движения",
    "C1": "надёжность ЖКХ",
    "C2": "скорость решения обращений",
}


EXPLANATION_FIELDS = {
    "valid",
    "score",
    "score_delta",
    "d_avg",
    "min_district",
    "n_crit",
    "district_scores",
    "changes",
    "critical_values",
    "decisions",
    "synergies",
    "total_cost",
    "budget",
    "budget_remaining",
    "horizon_quarters",
    "critical_threshold",
    "realized_contributions",
}


SYSTEM_PROMPT = """Ты объясняешь результат учебного симулятора городского бюджета.

Используй только факты и числа из переданного контекста. Не вычисляй новые значения,
не исправляй результаты движка и не придумывай отсутствующие данные. Не называй
сценарий оптимальным и не выдавай симуляцию за точный прогноз реального города.

Строго различай итоговые метрики: score — итоговый Astana Quality of Life Score,
score_delta — изменение итогового Score относительно базы, d_avg — средний балл
районов. Не называй score_delta изменением среднего балла города. Упоминай изменение
итогового Score только при наличии отдельного поля score_delta. Не вычитай значения
из base_result и не рассчитывай изменения агрегатов самостоятельно. Изменения
отдельных показателей бери только из поля changes.

Поле district_profiles описывает исходное состояние районов. Не утверждай, что
проблема из профиля сохранилась после сценария, если это прямо не подтверждено
результатами. Если n_crit равно 0, прямо укажи, что критических значений по порогу
симулятора не осталось, и не говори о нерешённых критических проблемах.

Если список synergies непустой, объясни смысл совместного эффекта. Каждую пару,
район и точный бонус приложение покажет само из контекста; не повторяй цифровые
коды и числа в комментарии. Если список пуст, скажи, что синергии не сработали.
Описывая компромиссы, опирайся только на стоимость, бюджет, выбранные направления
и фактические changes. Не утверждай, что выбор потребовал «жертв» или ухудшил
непрофинансированные области, если ухудшение не указано явно.

Приложение показывает числа в удобном для человека виде: максимум два знака после запятой,
без длинных машинных дробей. В разделе об улучшениях сначала объясни самые большие
положительные изменения из поля changes и не пропускай их ради меньших изменений.
Если n_crit равен 0 и critical_values пуст, прямо скажи, что критических значений
ниже порога не осталось. Не придумывай проблемы, причины, ограничения бюджета или
последствия для других районов, которых нет в контексте. Компромиссы описывай только
по явным отрицательным changes, critical_values, стоимости, лагам и данным выбранных
мер; при отсутствии таких данных честно скажи, что контекст не позволяет их оценить.

Ответь на русском языке тремя короткими разделами:
1. Что улучшилось.
2. Какие проблемы остались.
3. Какие компромиссы есть у выбранного набора.

Упоминай район, показатель или синергию только тогда, когда
это явно присутствует в контексте. Если данных недостаточно, прямо скажи об этом.

Формат ответа: JSON с тремя строковыми полями improvements, remaining_problems,
tradeoffs. В каждом — один-два коротких предложения человеческого объяснения.
Все числовые факты приложение покажет рядом само: НЕ пиши цифры, проценты,
числа словами, коды мер и показателей. Объясняй смысл готовых фактов без расчётов.
Не добавляй Markdown-заголовки или списки внутри полей.

total_cost, budget и budget_remaining — уже посчитанная стоимость, лимит и остаток.
decisions[].cost — цена отдельной меры; decisions[].lag — лаг в кварталах.
horizon_quarters — горизонт модели. Если эти поля заполнены, нельзя утверждать,
что стоимость, бюджет или лаги неизвестны. Лаг уменьшает долю эффекта в пределах
горизонта; это уже учтено в changes. Не называй исходные эффекты итоговыми дельтами.
Синергии уже включены в changes: не прибавляй бонус повторно.
realized_contributions — вклад каждой меры после лага, но до синергий и clip.
Это справка об источнике изменения, а не окончательное изменение района.
Окончательные дельты бери только из changes, не складывай вклады самостоятельно.
При наличии стоимости и лагов поясни их как компромисс в tradeoffs.
Нулевой n_crit означает отсутствие значений ниже порога, но не устранение всех
городских проблем. Минимальный балл района — относительное отставание в модели.

Конкретность ответа:
explanation_focus.top_improvements содержит уже отобранные кодом крупнейшие
улучшения с понятными названиями показателей. В improvements назови их районы
и смысл каждого показателя, включая самый большой прирост; не заменяй это
общими фразами «повысилось качество жизни» или «социальные показатели».
Можно написать до трёх коротких предложений, чтобы не потерять основные изменения.
В remaining_problems опирайся на critical_values и min_district: если критических
значений нет, скажи об этом и укажи относительное отставание слабейшего района.
Неизменившийся показатель сам по себе не доказывает наличие проблемы.
Не делай такой вывод даже с оговоркой «может свидетельствовать».
В tradeoffs объясняй долю использованного бюджета и отложенный эффект мер.
Лаг уже учтён: не обещай дополнительного снижения показанного результата.
Не называй меру неэффективной только из-за лага. Оставшийся бюджет не создаёт
новый слот решения: игровой набор всегда содержит ровно пять решений.
"""


def _for_explanation(value):
    """Create a display-only copy with readable floats; engine data stays untouched."""
    if isinstance(value, float):
        return round(value, 2)
    if isinstance(value, dict):
        return {key: _for_explanation(item) for key, item in value.items()}
    if isinstance(value, list):
        return [_for_explanation(item) for item in value]
    return value


def build_user_prompt(context: dict) -> str:
    """Serializes a human-readable copy of the already calculated context."""
    grounded_context = {
        key: context[key] for key in EXPLANATION_FIELDS if key in context
    }
    if "decisions" in grounded_context:
        # Raw catalogue effects are not the realized deltas after lag and clipping.
        decision_fields = {"id", "name", "direction", "type", "district", "cost", "lag"}
        grounded_context["decisions"] = [
            {key: value for key, value in decision.items() if key in decision_fields}
            for decision in grounded_context["decisions"]
        ]
    changes = context.get("changes") or []
    top_improvements = sorted(
        (row for row in changes if row["delta"] > 0),
        key=lambda row: row["delta"], reverse=True,
    )[:3]
    grounded_context["explanation_focus"] = {
        "top_improvements": [
            {**row, "indicator_name": INDICATOR_LABELS.get(row["indicator"], row["indicator"])}
            for row in top_improvements
        ],
        "observed_decreases": [row for row in changes if row["delta"] < 0],
    }
    payload = json.dumps(_for_explanation(grounded_context), ensure_ascii=False, sort_keys=True)
    return (
        "Ниже приведён результат, полностью рассчитанный детерминированным движком. "
        "Объясни его по правилам системного промпта.\n\n"
        f"КОНТЕКСТ СИМУЛЯЦИИ:\n{payload}"
    )
